Usage:
    bin/html-differ
        shows usage information
    bin/html-differ <firsr_html> <second_html>
        compares the performance of two html-files
    bin/html-differ -h
    bin/html-differ --help
        shows usage information
    bin/html-differ -v
    bin/html-differ --version
        shows the version number

Использование:
    bin/html-differ
        показывает этот текст
    bin/html-differ <первый_html> <второй_html>
        сравнивает работоспособность двух html-файлов
    bin/html-differ -h
    bin/html-differ --help
        показывает этот текст
    bin/html-differ -v
    bin/html-differ --version
        показывает номер версии
